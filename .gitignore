# System
.DS_Store
*~
*.swp

# Python
__pycache__/
*.py[cod]

# Geode Local State (if created inside repo)
.geode/
state/
secrets.env

# Personal / Site-Specific Data (Do Not Commit to Core)
# Personas: Ignore ALL by default, allow ONLY Core personas
personas/*
!personas/sys.md
!personas/general.md

# Knowledge Base
knowledge/

# User Tools: Ignore ALL by default, allow gitkeep so folder exists
user_tools/*
!user_tools/.gitkeep

# Backups
versions/

# User Config (should not be in repo, but just in case)
config.env
.geode_config
