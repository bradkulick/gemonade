# Community Packages

This directory is for installed community personas.
If you download a package (e.g., `git clone ...`), place it here.

**Structure:**
```text
packages/installed/
  └── aws-expert/
      ├── persona.md
      └── tools/
```
